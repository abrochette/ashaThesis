#!/bin/bash
# Load GTF data into database after deployment

set -e

# Source the environment
source /var/app/venv/*/bin/activate

cd /var/app/current

# Load GTF data (will skip if already loaded)
echo "Loading GTF data into database..."
python manage.py load_gtf_to_db

echo "GTF data loaded successfully!"

